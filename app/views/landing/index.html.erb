<%= link_to 'new article', 'articles/new'  %>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= article.title %><br />
      <%= article.content %><br />
      <p> Written by  <%= article.author %> at <%= article.created_at.strftime('%Y-%m-%d') %> </p>
    </li>
  <% end %>
</ul>
